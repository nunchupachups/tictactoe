//now let's add the color that we will need to project
import 'package:flutter/material.dart';

class MainColor {
  static Color primaryColor = Colors.black54;
  static Color secondaryColor = Color(0xFFD0C4BE);
  static Color accentColorX = Colors.red;
  static Color accentColorO = Colors.black;
}
